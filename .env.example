# Copy to .env and fill in real values; never commit the real .env.
# When unset, /predict is open (development mode); when set, callers must pass `token` matching this value.
API_TOKEN=change-me-in-prod
